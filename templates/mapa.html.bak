<!-- templates/about.html -->
{% extends 'base.html' %}
<!-- {% load leaflet_tags %}  -->

{% block content %}
<div class="HeightTaker container-fluid">
    <div class="HeightTaker container-fluid">
        <div class="Wrapper row">

            <div class="LeftNavigation col-md-auto">
                <ul class="nav flex-column">
                    <li class="nav-item">
                        <a class="nav-link active" href="#">Advanced Application</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#">Dział handlowy</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#">Serwis</a>
                    </li>
                </ul>

                <ol>
                    <li>PupA</li>
                    {% if tugger %}
<li>DipA</li>
                    {% endif %}
      {% for tug in tugger %}
        <li>{{ tug.Produkt }}</li>
      {% endfor %}
    </ol>

            </div>
            <div class=" col Content" id="mainmap">

            </div>
        </div>
    </div>
</div>
<!--
<script>

var map = L.map('mainmap', {
    minZoom: 0,
    maxZoom: 20
});


var googleStreets = L.tileLayer('http://{s}.google.com/vt/lyrs=m&x={x}&y={y}&z={z}',{
    maxZoom: 20,
    subdomains:['mt0','mt1','mt2','mt3']
}).addTo(map);

map.setView([52,18.6], 7);


</script>
 -->
{% endblock %}